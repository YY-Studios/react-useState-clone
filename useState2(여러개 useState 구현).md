일단 토끼래빗이 수정하면 좋겠다고 한거부터
"초기화 패턴을 넣어서 이벤트 리스너를 한 번만 등록하도록 하는 것을 추천드립니다.
onclick = 방식은 이전 핸들러를 덮어쓰기 때문에 리스너가 중복 실행되는 문제는 실제로 발생하지 않습니다
하지만 매번 렌더링할 때마다 새로운 함수 객체를 생성하고 할당하는 것은 비효율적입니다."

*초기화 패턴의 장점:*

1. 성능 개선: 이벤트 리스너를 한 번만 등록하므로 불필요한 함수 생성을 방지
2. 명확한 역할 분리:
- 초기 설정 (이벤트 리스너 등록) - 한 번만
- 렌더링 (상태 값 DOM에 반영) - 상태 변경 시마다
3. React 패턴 학습: 실제 React에서도 useEffect를 사용해 이벤트 리스너를 한 번만 등록하는 패턴을 사용합니다.

지금 근데 이거 넣으라는 게 살짝 헷갈리는데
onclick은 그러면 한번만 이렇게 등록해놓으면 원래도 그냥 실행이 되는건가?

[ai 자문] 그렇다네.. 그래서 이벤트가 여러번 실행되는게 아니라 렌더 될 때마다 새로 등록되는 비효율을 막을라고 초기화를 넣은거..

이건 리액트에서 useEffect와 같다고 한다
그러니까 좀 이해가 가는거 같기도 하고
useEffect도 뒤에 뎁스가 변하는걸 감지해야만 새로 랜더링을 하니께
이것도 뎁스 = 이벤트 이런느낌인건가?

-> 전혀 아니래요

- React의 useEffect deps
“어떤 상태가 바뀌면 실행해라. 그리고 빈 배열이면 딱 한 번만 실행해라.”

- 내 코드의 초기화패턴
“이미 했으면 다시 하지 마라. 그러니 사실상 ‘빈 배열 useEffect’와 동일하다.”

오키.. 그냥 useEffect(() => {…}, [])이거 자체인거임

----

이제 여러개의 useState를 만들어볼께요.

지금 상태를 arr에 넣고있는데 만약

**멘토님 코드 리뷰중..**

배열을 특별히 쓰신 이유가 있으실것같은데(순서를 저장할 자료구조?) 좋네요 👍🏻
useState 를 여러군대에서도 쓰이는걸 고려해서 arr[0] 말고 먼가 다른 처리를 해줘도 좋을것같숩니당 ㅎ
여러군대 쓰일때

```js
function App() {
  const [name, setName] = useState("Roberto");
  const [age, setAge] = useState(30);
  const [hobby, setHobby] = useState(0);
}
```

라고 달아주셨는데 그러면 이걸 전부 arr(훅)에서 상태 관리 를 해야하니
몇번째인지 알수있는 index가 필요하다.

```js
let 훅인덱스 = 0;
```
를 일단 만들었다.
그 다음 저걸 전부 useState에서 넣어서 arr로 리턴을 해야하는데

일케 되는거지

arr[0] = 이름 초기값, 이름 바뀐값
arr[1] = 나이 초기값, 나이 바뀐값
arr[2] = 취미 초기값, 취미 바뀐값

---

이제 useState 함수를 수정해보자

```js
const useState = (초기값) => {
  const state = arr[0] ?? 초기값;

  const setState = (새값) => {
    if (state !== 새값) {
      arr[0] = 새값;
      renderBox1();
    }
  };

  return [state, setState];
};
```

이렇게 되어있는데
조건문 돌려야지 일단 몇개있는지

근데 최대갯수 어떻게 알지?
돔에 그릴때 3개 넣는데
어떤거에 대한 length를 잡아야하지?
...................훔

필요없네
그냥 있는만큼 호출 할태니까
그냥 ++만 해줘서 arr에 리턴해주면 되는거임
이것도 setter가 훅인덱스가 몇인지 기억하는 클로저? [ai 자문 맞음]

---

```js
let arr = [];
let 훅_인덱스 = 0;

//useState만들기
const useState = (초기값) => {
  let 현재_인덱스 = 훅_인덱스
  const state = arr[현재_인덱스] ?? 초기값;

  const setState = (새값) => {
    if (arr[현재_인덱스] !== 새값) {
      arr[현재_인덱스] = 새값;
      renderBox1();
    }
  };

  arr[현재_인덱스] = state
  훅_인덱스 ++;
  return [state, setState];
};
```

이렇게 해봤어요
왜 여기서 외부에 훅_인덱스 = 0 을 잡고가는가..!
안에서 현재_인덱스 = 0 으로 시작하면 지역변수라서
한바퀴 돌아도 결국 0으로 다시 시작함

근데 외부에 0잡아놓으면 리턴으로 arr에 ++이 적용이 되고(클로저)
다시 돌때 그걸 가져오는거임

---

돔에 그릴때 중요한건

훅_인덱스 = 0; 처음에 이걸 선언해줘야함
왜냐면 계속 숫자가 쌓이니까

이걸 안하면 두번째 누를때
name  -> arr[3]
age   -> arr[4]
hobby -> arr[5]
이렇게 되어버릴수도 있어서

---

```js
//돔에 그리는 함수
const renderBox1 = () => {
  훅_인덱스 = 0;

  const [name, setName] = useState("Roberto");
  const [age, setAge] = useState(30);
  const [hobby, setHobby] = useState("");

  // 렌더링: 상태만 업데이트
  document.querySelector(".nameBox").textContent = name;
  document.querySelector(".ageBox").textContent = age;
  document.querySelector(".hobbyBox").textContent = hobby;

  if (!초기화패턴) {
    초기화패턴 = true;
    document.querySelector(".box1 button").onclick = () => {
      const nameInputVal = document.querySelector("#name").value;
      setName(nameInputVal);
      const ageInputVal = document.querySelector("#age").value;
      setAge(ageInputVal);
      const hobbyInputVal = document.querySelector("#hobby").value;
      setHobby(hobbyInputVal);
    };
  }
};
```

이렇게 했는데 초기값이 안나온다.. 그이유는 뭘까...
초기값뿐만이 아니라 렌더링도 안댐..

음..

뭔가 바로 렌더링이 되어서 초기값이 날라가는건가?
그러면 렌더링 되는 값이 라도 나와야하는거잖아.

HTML 오타를 내서 안나오는거였당..!






